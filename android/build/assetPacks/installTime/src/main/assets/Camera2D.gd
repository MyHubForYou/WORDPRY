extends Camera2D

#@export var $VFlowContainer: Node2D  # The object to center the camera on

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

#func _process(delta):
	#position = position.lerp($ANSWER.position, moveSpeed * delta)

#var moveSpeed = 1  # Adjust the movement speed as needed

#var targetPostion = $Marker2D.position

#var startingPostion = Vector2(0, 0)

#func _process(delta):
	#postion = startingPostion
	#postion = postion
	
	#if WINNER:
	#sposition = position.lerp($Marker2D.position, moveSpeed * delta)
